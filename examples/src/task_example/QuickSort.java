package task_example;

/*public class QuickSort {
    public static void quickSort(int[] array, int start, int end) { //индексы сегмента над которым работаем
        if (start >= end) {
            return;
        }
        int pivot = Partition(array, start, end);
        quickSort(array, 0, pivot - 1);
        quickSort(array, pivot + 1, array.length - 1);
    }

    *//*public static int Partition(int[] targerArray, int start, int end ) {

    }
}*/
